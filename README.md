# wwc_angularJS
Women who code AngularJS group project

Requires [Node.js](https://nodejs.org/en/download/).

## Install and run

```sh
$ cd client
$ npm install
$ cd ../server
$ npm install
$ node server
```